package com.baidu.dan.conan.server.writter;

public class MongoWritter {

	public final void doWork() {
		/*
		 * // // merge & get // RequestHandler.statisticsData.merge2Global();
		 * Double averageTime = RequestHandler.statisticsData.getAverageTime();
		 * 
		 * DB db = MongoService.getInstance().getDb("mydb"); DBCollection coll =
		 * db.getCollection("c1");
		 * 
		 * BasicDBObject info = new BasicDBObject(); info.put("test1",
		 * averageTime); coll.insert(info);
		 */
	}
}
